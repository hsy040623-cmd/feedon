Add common header to auth pages; add project delete

Login/signup/reset-password now use the shared SiteHeader (logo
link home + back button) instead of a static, non-navigable title,
so every page has consistent navigation.

Adds a delete (X) button next to each project in the project list
panel, backed by a new DELETE /api/projects/:id route that removes
the project's Storage objects (target files, reference images,
proposal files) before deleting the row (requests/proposals cascade
in the DB). Deleting the currently-open project redirects to
/projects.

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>
