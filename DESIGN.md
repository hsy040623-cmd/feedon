# DESIGN

## 1. 화면 구성

### 화면 1 — 요청 입력 화면 (홈)

| 위치 | 내용 |
|---|---|
| 상단 | 프로젝트(작업 건) 선택/생성 드롭다운 |
| 중앙 왼쪽 | 클라이언트 수정 요청 텍스트 입력창 |
| 중앙 왼쪽 아래 | 참고 이미지/스크린샷 업로드 버튼 (선택 사항) |
| 중앙 오른쪽 | 대상 파일 업로드 (워드·엑셀·파워포인트만 허용, 그 외 형식은 업로드 시 미지원 안내 후 차단) |
| 하단 | "분석 요청" 버튼 |

### 화면 2 — 반영 제안 미리보기 화면

| 위치 | 내용 |
|---|---|
| 상단 | 원본 vs 수정 제안 비교(전/후 나란히 표시) |
| 중앙 | AI가 추출한 수정 사양 요약 (무엇을 어떻게 바꿨는지 목록) |
| 하단 | "승인" / "반려" 버튼. 모호한 요청은 "확인 요청" 상태 표시와 함께 모호한 이유를 노출 (반려 사유는 별도로 기록하지 않음) |

### 화면 3 — 프로젝트(작업 건) 목록 화면

| 위치 | 내용 |
|---|---|
| 좌측 | 프로젝트(클라이언트별 작업 건) 목록 |
| 우측 | 선택한 프로젝트의 요청 이력 — 상태별(대기 / 분석중 / 확인 요청 / 제안 생성됨 / 승인됨 / 반려됨 / 실패) 목록 |

## 2. 데이터 흐름 (입력 → 처리 → 출력)

1. **입력**: 작업자가 화면 1에서 텍스트 요청 + (선택)이미지 + 대상 파일(워드/엑셀/파워포인트)을 제출
2. **처리**:
   - Next.js 서버(API 라우트)가 요청을 받아 상태를 "분석중"으로 표시
   - OpenAI API를 호출해 텍스트·이미지를 분석하고 `[수정 대상 위치 + 변경 전 + 변경 후]` 구조로 추출
   - 모호/상충 시 "확인 요청" 상태로 분류하고 여기서 멈춤
   - 분석이나 파일 처리 중 오류가 나면 "실패" 상태로 표시하고 재시도를 안내
   - 명확하면 대상 파일 형식에 맞는 문서 처리 라이브러리를 호출해 실제로 텍스트가 교체된 새 파일(반영 제안)을 생성
   - 원본 파일은 그대로 두고, 생성된 제안 파일과 요청 이력을 Supabase에 저장
3. **출력**:
   - 화면 2에 원본과 제안을 비교해서 보여줌
   - 작업자가 승인하면 제안 파일이 최종 산출물로 확정되어 다운로드 가능해짐
   - 반려하면 제안 파일만 폐기하고, 요청 이력(메타데이터)은 계속 남음
   - 모든 요청은 화면 3의 프로젝트별 이력에 상태와 함께 기록됨

## 3. 기술 선택

기본은 PRD.md에 정한 대로 **Next.js**로 고정하고, Vercel로 바로 배포할 수 있는 구성만 사용합니다.

| 기술 | 역할 | 쉬운 설명 |
|---|---|---|
| **Next.js** | 화면과 서버(API)를 한 프로젝트에서 처리 | 이미 my-app에 설치된 기본 틀. 화면도 그리고, 뒤에서 데이터 처리도 하는 도구 |
| **OpenAI API** | 텍스트·이미지 요청 분석 | 클라이언트가 적은 글과 첨부한 이미지를 "읽고 이해해서" 무엇을 고쳐야 하는지 알아내는 AI 두뇌 역할 |
| **문서 처리 라이브러리** (docx, xlsx, pptx용 각각 존재) | 워드/엑셀/파워포인트 파일 안의 글자를 찾아 바꾸는 도구 | 사람이 파일을 열어서 고치듯, 프로그램이 대신 파일을 열고 고칠 수 있게 해주는 부품. 1차로는 글자 교체만 지원하고, 이미지 교체·위치 조정은 난이도가 높아 이후 확장 과제로 둔다 |
| **Supabase** | 요청·프로젝트 이력, 업로드 파일, 반영 제안 파일을 저장 | 요청 기록과 파일을 안전하게 보관하는 창고. 화면 3에서 이력을 불러올 때도 여기서 꺼내옴 |
| **Vercel** | 완성된 앱을 인터넷에 올려서 실제로 접속 가능하게 배포 | Part 5에서 사용할 배포 서비스. Next.js와 가장 잘 맞음 |

**실제로 필요한 환경변수**: `OPENAI_API_KEY`(AI 분석용), `NEXT_PUBLIC_SUPABASE_URL` + `SUPABASE_SERVICE_ROLE_KEY`(서버 전용, Supabase 접속용). 지금 `.env`의 `SUPABASE_ACCESS_TOKEN`은 Supabase CLI 관리용 토큰이라, 앱이 실행 중에 데이터를 읽고 쓰려면 이 두 값을 프로젝트 설정에서 발급받아 별도로 추가해야 한다. `VERCEL_TOKEN`은 배포할 때 Vercel CLI 인증에만 쓰이고, 앱이 실행되는 동안에는 사용하지 않는다.

## 4. 보안·데이터 처리

- Supabase Storage 버킷은 비공개로 설정하고, 파일 업로드·다운로드는 서버(API 라우트)를 통해서만 이루어지게 한다.
- OpenAI API 호출 시 데이터가 모델 학습에 사용되지 않도록 설정하고, 전송하는 데이터는 분석에 필요한 최소한으로 제한한다.
- 요청·프로젝트 이력(메타데이터)은 계속 보관하되, 반려된 반영 제안의 실제 파일은 폐기한다.
- 1인 작업자 계정 사용을 전제로 하며, Supabase의 기본 인증 기능으로 로그인만 확인한다 (팀 권한 관리는 비범위).

## 5. 상태 값 정의

| 상태 | 의미 |
|---|---|
| 대기 | 요청이 제출되었지만 아직 분석 전 |
| 분석중 | AI가 텍스트·이미지를 분석하는 중 |
| 확인 요청 | 요청이 모호하거나 상충해 작업자 확인이 필요한 상태 |
| 제안 생성됨 | 반영 제안(Preview) 파일이 만들어져 검토 대기 중 |
| 승인됨 | 작업자가 승인해 최종 파일로 확정됨 |
| 반려됨 | 작업자가 반려해 제안 파일이 폐기됨 |
| 실패 | AI 분석 또는 파일 처리 중 오류 발생 |

## 6. API 명세 (간단)

| 엔드포인트 | 설명 |
|---|---|
| `POST /api/projects` | 프로젝트(작업 건) 생성 |
| `GET /api/projects` | 프로젝트 목록 및 상태별 요청 이력 조회 |
| `POST /api/requests` | 수정 요청 제출 (텍스트+이미지+대상 파일) → 분석 시작 |
| `GET /api/requests/:id` | 요청 상태 및 반영 제안 조회 |
| `POST /api/requests/:id/approve` | 반영 제안 승인 |
| `POST /api/requests/:id/reject` | 반영 제안 반려 |

응답 형식은 성공 시 `{ data: ... }`, 실패 시 `{ error: { code, message } }`로 통일한다. 주요 데이터: `project`(프로젝트), `request`(요청, 상태값 포함), `proposal`(반영 제안 파일 정보).

## 7. 제약 조건

- 업로드 파일 최대 용량: 20MB (대상 파일·참고 이미지 공통)
- 참고 이미지 허용 형식: PNG, JPG
- 대상 파일 허용 확장자: `.docx`, `.xlsx`, `.pptx` (그 외 업로드 시 즉시 차단하고 안내 메시지 표시)
- AI 분석은 Vercel 서버 함수의 실행 시간 제한 안에서 끝나야 하며, 시간이 오래 걸리는 경우의 처리 방식(예: 진행 상태 표시)은 이후 개발 단위에서 구체화한다.
