# CHECK

> PLAN.md "이번 사이클 성공 기준" 판정, DESIGN.md 대비 구현 Gap 분석, 공개 전 보안 점검(bkit security-architect)을 한 번에 정리한 문서. 점검일: 2026-09-17.

## 종합 판정: **실패**

PLAN.md에 적힌 6개 성공 기준 자체는 전부 통과했지만, 로그인을 선택 사항으로 바꾸면서 DESIGN.md의 핵심 전제("1인 작업자 로그인 확인")가 깨졌고, 보안 점검에서 **치명적 위험 3건**(무단 열람, 무단 삭제/승인/반려, 비용 무제한 남용)이 확인됐다. 이 상태로는 인터넷에 공개하기 안전하지 않으므로 종합 판정은 실패로 둔다.

## 1. 고쳐야 할 항목 (우선순위 순)

| # | 심각도 | 항목 | 출처 |
|---|---|---|---|
| 1 | 치명적 | 로그인이 선택 사항이 되며 데이터 소유자 구분이 사라져, 인터넷에 공개하면 누구나 모든 프로젝트·요청 텍스트·원본/제안 파일을 열람·다운로드할 수 있다 | Gap 1 / 보안 1 |
| 2 | 치명적 | 삭제·승인·반려 API(`/api/projects/:id` DELETE, `/api/requests/:id/approve`, `/reject`)에 인증 검사가 없어, 누구나 남의 프로젝트를 영구 삭제하거나 임의로 승인/반려할 수 있다 | 보안 2 |
| 3 | 치명적 | 요청 처리에 rate limit이 없고 참고 이미지 개수 제한도 없어, 누구나 OpenAI 호출 비용을 무제한으로 발생시킬 수 있다 | 보안 3 |
| 4 | 높음 | docx/xlsx/pptx 압축 해제 시 크기 검증이 없어, 압축 폭탄 파일 하나로 서버 메모리가 초과돼 죽을 수 있다 | 보안 4 |
| 5 | 높음 | 텍스트 치환이 XML 텍스트 노드가 아니라 문서 전체 문자열을 대상으로 해서, 악성이거나 우연한 입력으로 제안 파일(워드 등)이 열리지 않게 손상될 수 있다 | 보안 5-1 |
| 6 | 중요 | DESIGN.md 2번 "오류 시 재시도를 안내"가 미구현 — `FAILED`/`ANALYZING`에 멈춘 요청을 다시 시도할 버튼·API가 없다 | Gap 2 |
| 7 | 중요 | DESIGN.md 6번 API 명세의 `GET /api/requests/:id`가 실제로 구현되지 않았다(상세 화면은 서버 컴포넌트에서 직접 DB 조회) | Gap 3 |
| 8 | 중요 | DESIGN.md 화면2 "원본/제안 전후 나란히 비교"가 실제로는 다운로드 링크 2개뿐, 파일 내용을 비교 미리보기하는 기능은 없다 | Gap 4 |
| 9 | 중요 | DESIGN.md 7번 "AI 분석이 시간 제한을 넘길 때 진행 상태 표시"가 미구현 — 느리면 안내 없이 그냥 실패한다 | Gap 5 |
| 10 | 중간 | 내부 Supabase/OpenAI 에러 메시지가 API 응답에 그대로 노출되고, `/api/health`가 인증 없이 내부 연동 상태(원본 에러 포함)를 공개한다 | 보안 6 |
| 11 | 중간 | 개인정보 보관·삭제 정책이 없고, OpenAI로 데이터가 전송된다는 안내 문구가 없으며, 업로드 후 DB insert 실패 시 Storage에 고아 파일이 남는다 | 보안 7 |
| 12 | 낮음 | `robots.txt`가 없어 클라이언트 피드백 내용이 검색엔진에 색인될 수 있고, HSTS 등 보안 헤더도 없다 | 보안 8 |
| 13 | 낮음 | 비밀번호 최소 길이가 서버 설정(6자)과 화면 요구(8자)가 달라 API를 직접 호출하면 6자로도 가입되며, `/auth/confirm`의 `next` 파라미터 검증이 미흡하다 | 보안 9 |
| 14 | 낮음 | DESIGN.md 화면1의 "상단 프로젝트 선택/생성 드롭다운"이 드롭다운이 아니라 별도 페이지 이동(/start, /projects) 방식으로 구현됐다 | Gap 6 |
| 15 | 낮음 | DESIGN.md 4번 "업로드·다운로드는 서버 라우트를 통해서만"과 달리, 다운로드는 Supabase가 발급한 서명 URL로 Storage에서 직접 받는다 | Gap 7 |
| 16 | 낮음 | DESIGN.md 4번 "OpenAI 호출 시 모델 학습 미사용 설정"이 코드로 명시 설정된 바 없다(OpenAI 기본 정책에만 의존) | Gap 8 |
| 17 | 낮음 | 손상됐거나 실제 docx/xlsx/pptx가 아닌 파일을 올리면 JSZip 파싱 에러 메시지가 그대로 노출된다 | Gap 9 |
| 18 | 문서 최신화 | `DELETE /api/projects/:id`, 회원가입(`/signup`), 로그인 선택사항화 등 실제 구현된 기능이 DESIGN.md에 전혀 반영되어 있지 않다 | Gap 10 |

### 지금 바로(코드 수정 없이) 할 수 있는 것
- OpenAI 대시보드에서 월 사용량 한도(budget limit) 설정
- Supabase 콘솔에서 `minimum_password_length`를 8로 맞추기

## 2. PLAN.md 성공 기준 판정

| # | 성공 기준 | 판정 | 확인 방법 |
|---|---|---|---|
| 1 | 텍스트+이미지 요청 → AI가 수정 대상·내용 추출 | 충족 | 워크스페이스에서 텍스트(+이미지)와 파일을 함께 제출 → 상세 화면 "AI가 추출한 수정 사항"에 위치/전/후가 나오는지 확인 |
| 2 | docx/xlsx/pptx 세 형식 모두 반영 제안 자동 생성 | 충족 | 세 확장자 파일을 각각 올려 제출 → "제안 파일 다운로드"로 받은 파일을 열어 텍스트가 실제로 바뀌었는지 확인 |
| 3 | 승인 전까지 원본 파일 불변 | 충족 | "원본 다운로드"로 받은 파일을 처음 업로드한 파일과 비교(승인/반려 여부와 무관하게 항상 동일해야 함) |
| 4 | 승인 시 최종 반영, 반려 시 제안 파일만 폐기 + 이력 유지 | 충족 | 하나는 승인, 하나는 반려 → 승인 건은 "최종 파일 다운로드" 노출, 반려 건은 파일 폐기 문구 + 이력에는 그대로 남는지 확인 |
| 5 | 미지원 형식(어도비/한글/Figma 등) 업로드 시 안내+차단 | 충족 | 대상 파일로 `.hwp`, `.psd` 등을 선택 → "지원하지 않는 파일 형식이에요" 메시지와 함께 제출이 막히는지 확인 |
| 6 | `npm run lint`, `npm run build` 오류 없이 통과 | 충족 | 터미널에서 `npm run lint`, `npm run build` 직접 실행해 exit code 0 확인 |

## 3. DESIGN.md vs 구현 Gap 목록 (상세)

1. **[Critical]** DESIGN 4번의 "1인 작업자 계정 + 로그인 확인" 전제가 뒤집혀, 로그인 없이 누구나 모든 프로젝트·요청·파일에 접근·수정·삭제 가능(데이터 격리 없음, `projects`에 소유자 컬럼 자체가 없음).
2. **[Critical]** DESIGN 2번 "오류 시 재시도를 안내"가 지켜지지 않음 — FAILED 상태 요청을 재시도할 UI·API가 없고, ANALYZING에서 멈춘 요청도 자동 복구/타임아웃 처리가 없음.
3. **[Important]** DESIGN 6번 API 명세의 `GET /api/requests/:id`가 구현되지 않음(상세 화면은 API 대신 서버 컴포넌트에서 직접 DB 조회).
4. **[Important]** DESIGN 화면2 "원본 vs 수정 제안 비교(전/후 나란히 표시)"가 실제로는 다운로드 링크 2개 + AI가 추출한 텍스트 목록뿐, 파일 내용을 나란히 미리보기하는 기능은 없음.
5. **[Important]** DESIGN 7번 "AI 분석이 Vercel 함수 시간 제한을 넘길 때의 진행 상태 표시"가 미구현 — 전체 파이프라인이 동기 처리라 대용량 파일/느린 분석 시 타임아웃되면 아무 안내 없이 실패.
6. **[Minor]** DESIGN 화면1 "상단 프로젝트 선택/생성 드롭다운"이 드롭다운이 아니라 별도 페이지(/start, /projects) 이동 방식으로 구현됨.
7. **[Minor]** DESIGN 4번 "파일 업로드·다운로드는 서버(API 라우트)를 통해서만"과 달리, 다운로드는 서버가 생성한 서명 URL로 Supabase Storage에서 직접 내려받음(업로드는 서버 경유 맞음).
8. **[Minor]** DESIGN 4번 "OpenAI API 호출 시 데이터가 모델 학습에 사용되지 않도록 설정"이 코드로 명시적으로 설정된 바 없음(OpenAI 기본 API 정책에 의존).
9. **[Minor]** 손상되었거나 실제로 docx/xlsx/pptx 형식이 아닌 파일을 업로드하면 JSZip 파싱 에러 메시지가 그대로 노출되어 사용자 친화적이지 않음.
10. **[문서 최신화 필요]** `DELETE /api/projects/:id`, 회원가입(/signup), 로그인 선택사항화 등은 DESIGN.md에 없는 기능으로, 문서가 실제 구현을 따라가지 못해 최신 상태가 아님.

## 4. 보안 점검 결과 (bkit security-architect, 상세)

### 확인해보니 괜찮았던 것
- 하드코딩된 키 없음, 서비스 롤 키·OpenAI 키는 서버 전용 파일에서만 사용되고 클라이언트 번들에 노출되지 않음(브라우저로 새는 건 원래 공개용인 `NEXT_PUBLIC_SUPABASE_URL`/`ANON_KEY`뿐).
- `dangerouslySetInnerHTML`/`innerHTML`/`eval` 사용 0건 — XSS 경로 없음.
- SQL은 전부 supabase-js 쿼리 빌더만 사용 — SQL 인젝션 경로 없음.
- Storage 키가 `requests/{UUID}/target.{검증된확장자}` 형태라 파일명·프로젝트 이름으로 경로 탈출 불가능, 다운로드 파일명도 `encodeURIComponent`로 안전하게 처리.
- `.gitignore`에 `.env*`, `.vercel` 모두 등록됨.

### 발견된 위험 (심각도 순)
1. **[치명적]** 아무나 모든 고객사 피드백·파일을 볼 수 있음 — `/api/projects` GET이 전체 목록을 반환하고, `/projects`·요청 상세 화면에서 UUID 추측 없이 모든 프로젝트의 원본/제안 파일 다운로드 링크를 받을 수 있음.
2. **[치명적]** 아무나 남의 프로젝트를 영구 삭제하거나 제안을 승인/반려할 수 있음 — DELETE/approve/reject 라우트에 "누가 요청했는지" 확인이 0줄. CSRF 방어도 없음.
3. **[치명적]** rate limit이 전무하고 참고 이미지 개수 제한도 없어, 누구나 OpenAI 비용을 무제한으로 발생시킬 수 있음(이미지 개수는 크기·형식만 검사하고 개수는 검사하지 않음).
4. **[높음]** docx/xlsx/pptx 압축 해제 크기를 검증하지 않아 압축 폭탄(zip bomb) 파일로 서버 메모리 초과를 유발할 수 있음.
5. **[높음]** 프롬프트 인젝션의 실질 피해는 원격 코드 실행이 아니라 "제안 파일 손상"(치환이 XML 텍스트 노드가 아닌 문서 전체 문자열 대상) 및 작업자 화면에 가짜 안내 문구를 띄우는 정도로 제한적이지만 실재함. AI 응답 항목의 타입 검증도 없어 예외 메시지가 그대로 화면에 노출될 수 있음.
6. **[중간]** Supabase/OpenAI 내부 에러 메시지가 API 응답에 그대로 노출됨. `/api/health`는 인증 없이 누구나 호출 가능하고 실패 시 원본 에러까지 반환하며, 호출마다 실제 OpenAI API를 때림.
7. **[중간]** 개인정보 보관·삭제 정책 없음(보관 기간 개념 자체가 없음), OpenAI로 데이터가 전송된다는 안내 문구 없음, 업로드 후 DB insert 실패 시 Storage에 고아 파일이 남아 무한히 쌓일 수 있음.
8. **[낮음~중간]** `robots.txt`/`sitemap.xml` 없음(검색엔진에 고객 피드백이 노출될 위험), HSTS·CSP 등 보안 헤더 없음.
9. **[낮음]** `/auth/confirm`의 `next` 파라미터 검증 미흡(오픈 리다이렉트까지는 아님), Supabase `minimum_password_length`(6)와 화면 요구(8자) 불일치, captcha 없이 무제한 회원가입 가능(현재는 계정에 권한이 없어 피해 없음, 향후 계정 기반 접근제어 도입 시 재검토 필요).

### 지금 바로 할 수 있는 것
1. 목록 나열을 막거나 소유자 개념 도입 (항목 1) — 가장 중요
2. 삭제·승인·반려에 로그인 검사 3줄 추가 (항목 2)
3. OpenAI 대시보드 월 사용량 한도 + 이미지 개수 상한 (항목 3)
4. `robots.ts`로 전체 noindex 설정 (항목 8) — 항목 1이 해결되기 전까지
5. `/api/health` 삭제 또는 차단 (항목 6)
