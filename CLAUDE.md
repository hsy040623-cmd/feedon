@AGENTS.md

# 프로젝트 규칙 (CLAUDE.md)

## 프로젝트 개요

클라이언트가 전달한 텍스트/이미지 수정 요청을 AI가 분석해, 디자인·문서 파일에 자동으로 반영 "제안"까지 만들어주는 피드백 처리 도구. 대상 사용자는 프리랜서 디자이너·개발자, 외주 제작 에이전시. 자세한 기획은 [PRD.md](PRD.md) 참고.

## 기술 스택

- Next.js (TypeScript, App Router, `src/` 디렉토리, Tailwind CSS, import alias `@/*`)
- 프레임워크 버전이 최신이라 학습 데이터와 다를 수 있음 — 프레임워크 관련 코드를 짜기 전 `node_modules/next/dist/docs/`를 확인한다 (위 AGENTS.md 규칙 그대로 적용)
- AI 분석: OpenAI API (`OPENAI_API_KEY`)
- 배포: Vercel (`VERCEL_TOKEN`)
- 데이터: Supabase (`SUPABASE_ACCESS_TOKEN`)
- 기술 스택은 PRD.md에 정한 대로 **Next.js로 고정**한다. 다른 프레임워크로 바꾸거나 마이그레이션을 제안하지 않는다. 배포는 **Vercel**을 사용한다.

## 지원 범위

- 지원: Figma, 워드(.docx), 엑셀(.xlsx), 파워포인트(.pptx)
- 미지원: 포토샵·일러스트레이터·인디자인 등 어도비 프로그램, 한글(.hwp) — 공식 API 부재/불안정으로 제외 (PRD.md 6번 항목)

## 핵심 동작 원칙

1. AI가 만든 수정안은 항상 "제안(Preview)" 상태로만 생성하고, 작업자가 승인하기 전까지 원본 파일을 덮어쓰지 않는다.
2. 요청 텍스트·이미지가 모호하거나 서로 상충하면 임의로 해석해 반영하지 않고 "확인 요청" 상태로 분류한다.
3. 반려된 제안은 별도 사유 기록 없이 처리를 종료한다.

## 보안 규칙

- `.env`의 비밀 값(GITHUB_TOKEN, SUPABASE_ACCESS_TOKEN, VERCEL_TOKEN, OPENAI_API_KEY)은 코드에 하드코딩하거나 로그로 출력하지 않는다.
- `.env`, `node_modules`는 항상 `.gitignore`에 등록해 두고, 절대 커밋하지 않는다.
- 클라이언트 요청·파일 데이터는 비공개 등급으로 취급하고, 광고·모델 재학습 등 목적 외 용도로 사용하지 않는다.
- 외부 서비스 인증이 필요하면 토큰 값을 사용자에게 묻거나 채팅에 출력하지 말고, `.env`에 있는 값을 읽어서 사용한다.
  - 예: Supabase 작업이 필요하면 Supabase CLI를 설치해 `.env`의 `SUPABASE_ACCESS_TOKEN`으로 작업한다.
  - 예: Vercel 작업(배포 등)이 필요하면 Vercel CLI를 설치해 `.env`의 `VERCEL_TOKEN`으로 인증해 작업한다.

## 개발 순서

PRD.md 맨 아래 "개발 단위" 섹션의 순서(기본 세팅 → 요청 입력 → AI 분석 → 포맷별 반영 → 승인 흐름 → 프로젝트 구분 → 보안 → 배포)를 따른다.

## 코드 스타일

- ESLint·Tailwind 설정을 따른다.
- 지금 필요한 기능만 구현하고, 당장 쓰이지 않는 추상화·설정은 만들지 않는다.
- 모든 설명과 주석은 한국어로 작성한다.

## 작업 규칙

- 새 파일은 my-app 폴더 안에만 만든다.
- 코드를 바꾸면 반드시 무엇을 왜 바꿨는지 한 줄로 알려준다.
- 파일을 지워야 할 때는 바로 삭제하지 않고, `trash-can` 폴더를 만들어 그 안으로 옮겨만 둔다. 작업이 끝난 뒤 사용자가 직접 확인하고 삭제한다.
- 이미 설치된 서브에이전트는 필요할 때마다 적극 활용한다.

## 작업 절차 (검증 루프)

코드를 바꿀 때마다 아래 순서를 통과할 때까지 반복한다.

1. 변경한다.
2. `npm run lint`와 `npm run build`를 실행해 오류가 없는지 확인한다.
3. 화면이 바뀐 작업이면 `npm run dev`로 띄워 실제로 보이는지 확인한다.
4. 문제가 있으면 고치고 다시 1)로 돌아간다.
5. 통과하면 무엇을 왜 바꿨는지 한 줄로 요약한다.
