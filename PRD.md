# 피드백 반영 도우미 서비스 기획서 (PRD)

- 작성자: dana
- 작성일: 2026-09-16

## 1. 추진 배경 (문제 정의)

프리랜서 디자이너·개발자, 그리고 외주 제작을 담당하는 에이전시 팀은 프로젝트 진행 중 클라이언트로부터 텍스트 형태의 수정 요청(예: "이 문구를 이렇게 바꿔주세요", "이 부분 삭제해주세요")을 수시로 받는다. 이러한 요청은 이메일, 메신저, 문서 코멘트 등 비정형 텍스트로 전달되는 경우가 많아, 작업자가 요청 내용을 일일이 읽고 해석한 뒤 디자인·문서 파일에서 해당 부분을 찾아 수동으로 반영해야 한다. 요청 건수가 많아지거나 여러 클라이언트를 동시에 응대할 때는 이 과정에서 누락·오반영이 발생하고, 반영 완료까지 걸리는 시간도 늘어난다.

## 2. 현행 방식과 한계

현재는 클라이언트의 수정 요청을 받으면 작업자가 이메일·메신저·문서 코멘트를 직접 읽고, 어떤 파일의 어느 부분을 바꿔야 하는지 스스로 판단한 뒤 디자인 툴이나 문서 편집기에서 수동으로 하나씩 반영한다. 요청이 여러 건 쌓이거나 여러 클라이언트를 동시에 응대할 경우, 어떤 요청을 반영했고 안 했는지 추적하기 어려워지며, 반영을 빠뜨리거나 요청 의도를 잘못 해석해 엉뚱한 부분을 수정하는 실수가 발생한다. 또한 매번 사람이 직접 읽고 판단해야 하므로, 처리 시간이 요청자의 숙련도와 여유 시간에 크게 좌우된다.

## 3. 목표와 기대효과 (성공 기준)

- 클라이언트 수정 요청 1건당 작업 소요 시간을 기존 대비 **80% 이상 단축**한다.
- 요청 누락 및 오반영 건수를 **월 1건 이하**로 제어한다.
- AI 추출 및 반영 정확도: AI가 해석 및 자동 반영한 수정안에 대한 작업자의 최종 승인율(손수 재수정 없는 비율) **80% 이상**을 확보한다.

## 4. 사용자와 이용 흐름

**사용자**: 프리랜서 디자이너·개발자, 외주 제작 에이전시의 작업자

**이용 흐름**:
클라이언트가 텍스트로 수정 요청 전달 → 작업자가 도구에 요청 텍스트 붙여넣기/입력 → AI가 요청을 분석해 수정 항목 추출 → 대상 디자인/문서 파일에 반영 제안 자동 생성 → 작업자가 반영 제안 확인 후 승인 또는 반려 → 승인 시 최종 파일에 반영 완료

## 5. 주요 기능 (must / nice로 구분)

**Must (최대 2개)**

1. **수정 요청 텍스트·이미지 분석**
   - AI가 지킬 규칙: 입력된 텍스트와 이미지에서 "수정 대상(무엇을) + 수정 내용(어떻게)"을 쌍으로 추출한다. 대상이나 내용 중 하나라도 명확하지 않으면 자동 반영하지 않고 작업자에게 "확인 요청" 상태로 알린다.
2. **디자인/문서 파일 자동 반영**
   - AI가 지킬 규칙: 추출된 수정안은 원본 파일을 직접 덮어쓰지 않고, 반영 "제안" 상태로 생성한다. 작업자가 승인하기 전까지는 최종 파일에 확정 반영되지 않는다.

**Nice to have**

- 요청-반영 매칭에 대한 신뢰도 점수 표시

(반영 이력 조회는 6번 범위의 "프로젝트별 요청 구분·이력 확인" 기능에 이미 포함되어 있어 별도 nice-to-have에서 제외함)

## 6. 범위와 비범위

**이번에 만들 것 (범위)**

- 텍스트로 전달된 수정 요청을 분석해 수정 항목을 추출하는 기능
- 추출된 수정안을 디자인/문서 파일에 "제안" 형태로 반영하는 기능
- **텍스트 및 이미지/스크린샷 기반 수정 요청 파싱**
  - 기능 설명: 클라이언트가 전달한 비정형 텍스트 요청뿐만 아니라, 원하는 방향이 표시된 예시 이미지나 스크린샷(예: 영역 표시, 텍스트 가이드 이미지)을 분석하여 정확한 수정 사양을 추출합니다.
  - AI가 지킬 규칙:
    - 입력된 텍스트와 이미지 분석 결과를 종합하여 `[수정 대상 위치 + 변경 전 내용 + 변경 후 내용/요청 사항]`을 하나의 구조화된 데이터 쌍으로 추출한다.
    - 텍스트와 이미지의 내용이 상충하거나, 시각적 지시 사항이 모호할 경우 임의로 해석해 반영하지 않고 작업자에게 확인 요청 상태로 분류한다.
- **대상 디자인/문서 파일 자동 반영 제안**
  - 지원 파일 형식: **워드(.docx), 엑셀(.xlsx), 파워포인트(.pptx)** — 공식 API/라이브러리로 확실히 구현 가능한 형식만 1차 범위로 한정한다.
  - 기능 설명: 추출된 수정 사양을 바탕으로 대상 파일의 해당 텍스트를 찾아 교체하는 것을 1차 범위로 하며, 이미지 대체·위치 조정은 난이도가 높아 이후 확장 과제로 둔다.
  - AI가 지킬 규칙:
    - 추출된 수정안은 원본 파일에 즉시 덮어쓰지 않고 항상 "반영 제안(Preview)" 상태로 생성한다.
    - 작업자가 결과를 눈으로 확인하고 최종 승인 버튼을 누르기 전까지는 원본 파일이 변경되지 않도록 한다.
- 지원하지 않는 파일 형식 업로드 시 안내 후 차단하는 기능
- 작업자가 제안을 확인하고 승인/반려하는 기능 (반려 시 별도 사유 기록 없이 처리 종료. AI가 생성한 제안을 작업자가 직접 편집하는 기능은 제공하지 않는다)
- 프로젝트(작업 건)별로 요청을 구분하고, 상태별 이력을 확인하는 기능 (클라이언트/프로젝트 단위로 공간을 나눠 요청과 진행 상태를 모아봄)

**절대 만들지 않을 것 (비범위)**

- 클라이언트와의 실시간 채팅/커뮤니케이션 기능 자체 (요청 전달은 기존 채널을 그대로 사용, 이 도구는 반영만 담당)
- 여러 명이 동시에 승인 권한을 갖는 팀 협업·권한 관리 기능 (1인 작업자 계정을 전제로 한다)
- 디자인/문서 파일을 처음부터 새로 만들어주는 기능 (기존 파일 수정만 지원)
- 포토샵·일러스트레이터·인디자인 등 어도비 프로그램 파일 지원 (공식 API 부재 또는 별도 데스크톱 플러그인 개발이 필요해 이번 범위에서 제외)
- 한글(.hwp) 파일 지원 (비공개 포맷으로 자동 처리 도구가 불안정해 이번 범위에서 제외)
- 피그마(Figma) 파일 지원 (Figma의 공식 API는 읽기 전용이라, 실제로 파일을 자동 수정하려면 Figma 앱 안에서 도는 별도 플러그인 개발이 필요해 이번 범위에서 제외 — 추후 확장 검토)
- AI가 생성한 반영 제안을 작업자가 직접 편집하는 기능 (승인/반려만 지원)

## 7. 보안·개인정보 검토

- **공개등급**: 클라이언트의 수정 요청 텍스트·이미지 및 디자인/문서 파일은 외부 공개 금지(비공개/사내 한정) 등급으로 취급한다.
- **개인정보 포함 가능성**: 클라이언트 요청 텍스트나 첨부 이미지에 고객사의 민감 정보(연락처, 미공개 프로젝트명 등)가 포함될 수 있으므로, 별도 목적(광고, 모델 재학습 등)으로 저장·활용하지 않는다.
- **API 키 관리**: AI 분석에 사용하는 API 키 등 비밀 값은 `.env` 파일에 저장하고 `.gitignore`로 관리하여 GitHub 등 외부에 노출되지 않도록 한다.
- **데이터 보관**: 요청·프로젝트 이력(메타데이터)은 별도 삭제 절차 없이 계속 보관한다. 다만 반려된 반영 제안의 실제 파일은 폐기하고, 승인된 파일만 보관한다.
- **접근 주체**: 1인 작업자 계정 사용을 전제로 하며, 별도의 팀 권한 관리는 하지 않는다 (6번 비범위와 연동).

## 8. 기술 스택

- **Next.js**: 화면(프론트엔드)과 서버 쪽 코드(API)를 한 프로젝트에서 함께 처리한다.
- **OpenAI API**: 클라이언트의 텍스트·이미지 수정 요청을 분석해 수정 대상/내용을 추출한다.
- **Supabase**: 요청·프로젝트 이력과 업로드된 파일, 반영 제안을 저장한다.
- **Vercel**: 완성된 앱을 배포해 실제로 접속할 수 있게 한다.
- **고정 이유**: STEP 2에서 my-app 프로젝트에 Next.js가 이미 설치되어 있고, 이후 Vercel로 배포할 예정이라 그대로 이어서 사용하기 위함이다. 나머지 기술 구성은 DESIGN.md 3번 항목에서 정한 대로 따른다.

## 개발 단위

세부 개발 순서는 [PLAN.md](PLAN.md)를 따른다 (개발 순서가 두 문서에 중복되면 서로 어긋날 수 있어, PLAN.md 한 곳에서만 관리한다).
