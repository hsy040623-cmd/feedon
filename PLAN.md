# PLAN

## 이번 사이클 목표

PRD.md에 정의된 "피드백 반영 도우미"의 핵심 기능(Must 2개 + 범위에 포함된 3개 파일 형식 지원)을 동작하는 최소 버전으로 구현한다.

## 성공 기준

- 클라이언트 요청(텍스트+이미지)을 입력하면 AI가 수정 대상·내용을 추출한다.
- 워드(.docx), 엑셀(.xlsx), 파워포인트(.pptx) 세 형식 모두에 대해 반영 "제안(Preview)"이 자동 생성된다.
- 작업자가 승인하기 전까지 원본 파일이 변경되지 않는다.
- 승인 시 최종 파일에 반영되고, 반려 시 제안 파일만 폐기되며 요청 이력은 남는다.
- 지원하지 않는 파일 형식(어도비/한글/Figma 등)은 업로드 시 안내와 함께 차단된다.
- `npm run lint`, `npm run build`가 오류 없이 통과한다.

## 작업 순서 (의존성 우선)

1. 요청·프로젝트·제안 공통 데이터 모델 정의 (프로젝트/요청/제안 엔티티, 상태값: 대기·분석중·확인요청·제안생성됨·승인됨·반려됨·실패)
2. Next.js API 라우트 기본 구조 구성, OpenAI API 연동 확인, Supabase 프로젝트·테이블·Storage 버킷 설정
3. 프로젝트(작업 건) 생성·선택 기능 구현
4. 클라이언트 수정 요청 입력 UI 구현 (텍스트 입력 + 참고 이미지 업로드 + 대상 파일 업로드, 워드/엑셀/파워포인트 외 형식은 업로드 차단)
5. AI 텍스트 분석 — 수정 대상·내용 추출 로직 구현
6. AI 이미지 분석 결합 — 텍스트+이미지 종합 추출 로직 구현
7. 모호하거나 상충하는 요청에 대한 "확인 요청" 처리 로직 구현
8. 반영 제안(Preview) 공통 처리 구조 구현 (원본 불변, 승인 전 임시 상태 유지)
9. 워드(.docx) 반영 제안 생성 기능 구현
10. 엑셀(.xlsx) 반영 제안 생성 기능 구현
11. 파워포인트(.pptx) 반영 제안 생성 기능 구현
12. 반영 제안 미리보기 화면(작업자 확인 UI) 구현
13. 승인/반려 처리 기능 구현 (승인 시 최종 파일 저장, 반려 시 제안 파일 폐기)
14. 프로젝트별 요청 이력 화면(상태별 목록) 구현
15. 보안 점검 (API 키 노출 방지, Supabase 비공개 Storage 설정, 최소 인증 적용, 데이터 보관·폐기 정책 적용)
16. 전체 흐름 통합 테스트 및 Vercel 배포
